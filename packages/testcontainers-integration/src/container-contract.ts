/** Connection or runtime data returned after a managed container starts. */
export interface ContainerResource {
  /** Stable kind used to store and retrieve the resource. */
  readonly kind: string;
}
